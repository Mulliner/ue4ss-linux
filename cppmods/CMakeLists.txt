# Enable IDE organization with folders
set_property(GLOBAL PROPERTY USE_FOLDERS ON)

# Add C++ mods
# EventViewerMod and KismetDebuggerMod require ImGui (GUI-enabled only)
if(UE4SS_GUI_ENABLED)
    add_subdirectory("KismetDebuggerMod")
    add_subdirectory("EventViewerMod")
endif()

# IsleCPPUtilities has no UI and runs on a headless dedicated server, so it is
# deliberately outside the GUI guard above.
add_subdirectory("IsleCPPUtilities")

# Organize targets in the "mods" folder
get_property(TARGETS DIRECTORY ${CMAKE_CURRENT_SOURCE_DIR} PROPERTY BUILDSYSTEM_TARGETS)
foreach(target ${TARGETS})
    set_target_properties(${target} PROPERTIES FOLDER "mods")
endforeach()
